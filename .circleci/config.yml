version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: sudo npm install -g npm@latest
      - run: npm ci
      - run: npm run lint
      - run: npm run test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build

